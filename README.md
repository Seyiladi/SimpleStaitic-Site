# SimpleStaitic Site
Static Site For Workflow Learning
## What I added

- A complete, responsive `index.htm` portfolio for a religious studies teacher.
- A simple `styles.css` with responsive layout, accessible colors, and print rules.

## How to view locally

Open `index.htm` in your browser (no server required). On Windows you can double-click the file or run:

```powershell
# From the repository folder
Start-Process index.htm
```

## Next steps (optional)

- Replace the placeholder name and contact details in `index.htm`.
- Add a `cv.pdf` and update the CV link.
- Add sample lesson plans or images inside an `assets/` folder.

---
Generated on update by an automated assistant.
